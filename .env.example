# Template: copy to `.env` at repo root and fill in (never commit real keys).

# OpenAI (`scripts/generate_clap_descriptions.py --provider openai`)
OPENAI_API_KEY=

# Gemini (`--provider gemini`): https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Same Gemini key accepted under this name
# GOOGLE_API_KEY=
